<!DOCTYPE html>
<html lang="es-MX">
    <head>
        <title>Login</title>
        <meta charset="utf-8" name="viewport" content="width=device-width, initial-scale=1">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css"> <!-- pagina de estilos recomendada en el Ejercicio -->
        <link rel="preconnect" href="https://fonts.gstatic.com">   <!-- Documentos de estilo de letra ROBOTO -->
         <link href="https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,300;1,100&display=swap" rel="stylesheet">

        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.11.2/css/all.min.css">
        <link rel="preconnect" href="https://fonts.gstatic.com">
        <link rel="stylesheet" href="/css/styles.css">
        <link rel="stylesheet" href="/css/login.css">
        
    </head>


    <body>
        <%- include('../partials/header') %>
    <main class="m1">
        <div class="login">LOGIN</div>
        
        <form class="fform " action="/login" method="POST">

            <div>
                <input class="formu" type="email" name="correo" id="correo" placeholder="Correo" required>
            </div>
            <% if(locals.usuarioNoLogeado){ %>  <!-- Si la variable "usuarioLogeado viene vacia, NO ENTRA"-->
            <p  class="alineadoNoLogeado"><%= usuarioNoLogeado %></p>
             <% } %>  

            <div>
                <input class="formu"type="password" name="contraseña" id="contraseña" placeholder="Contraseña" required>
            </div>
            <% if(locals.passwordNoCorrecto){ %>  <!-- Si la variable "usuarioLogeado viene vacia, NO ENTRA"-->
            <p  class="alineadoNoLogeado"><%= passwordNoCorrecto %></p>
             <% } %>  



            <div class="sesion">
                <label><input type="checkbox" id="1check"> Mantener sesión iniciada</label>
            </div>

            <div class="boton"> 
                <button class="bbutton" type="submit">Ingresar</button>
            </div>

            <div class="olvido"><a class="aa" href="/">¿Olvidaste tu contraseña?</a></div>

            <div class="olvido"><a class="aa" href="/register">No tienes cuenta, Registrate AQUI</a></div>

        </form>
    </main>

    </body>
    <%- include('../partials/footer') %>
</html>